# 🦙 LOCAL OLLAMA CONFIGURATION (No API keys needed!)
USE_OLLAMA=true
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=codellama:7b
OLLAMA_TIMEOUT=180

# 📦 BITBUCKET CONFIGURATION (Required for repository operations)
BITBUCKET_USERNAME=your_bitbucket_username
BITBUCKET_TOKEN=your_bitbucket_app_password

# ⚙️ APPLICATION SETTINGS
LOG_LEVEL=INFO
ENVIRONMENT=development
MAX_FILE_SIZE=10485760

# 🤖 AI SETTINGS
LLM_MODEL=ollama-codellama
LLM_TEMPERATURE=0.1
MAX_TOKENS=4000

# 🔧 REPOSITORY SETTINGS
DEFAULT_BRANCH=main
PR_BRANCH_PREFIX=vulnfixer
SUPPORTED_REPOS=bitbucket,github

# 🛠️ DEVELOPMENT OPTIONS
SKIP_VALIDATION=true

# Optional: GitHub token for enhanced fixed version resolution
GITHUB_TOKEN=your_github_token_here

# Optional: Enable/disable the enhancement feature
ENABLE_FIXED_VERSION_RESOLUTION=true

# Optional: Timeout for external API calls
FIXED_VERSION_TIMEOUT=10
